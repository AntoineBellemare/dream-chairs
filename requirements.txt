# Core pipeline deps (torch assumed preinstalled with CUDA — you have 2.5.1+cu124)
numpy
scipy
scikit-learn
pyyaml
tqdm
pillow
trimesh

# CLIP text/image encoder (mirrors the existing CLIP descriptor-matching approach)
open_clip_torch
ftfy
regex

# OpenShape backend (native 3D<->text). The encoder is vendored DGL-free, so DGL is
# NOT required. openai-clip (ViT-L/14 text) comes in via shap-e; listed here for clarity.
torch-redstone
einops
huggingface_hub
# clip @ git+https://github.com/openai/CLIP.git   # provides ViT-L/14 text for vitl14

# --- 3D generation (install separately; pulled from GitHub) ---
# pip install git+https://github.com/openai/point-e.git
# pip install git+https://github.com/openai/shap-e.git
#
# torch is NOT pinned here so the existing CUDA build is not clobbered.
# If you ever need to reinstall it:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
